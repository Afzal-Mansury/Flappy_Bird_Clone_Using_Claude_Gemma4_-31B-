const CONFIG = {
    GRAVITY: 0.15,
    JUMP_FORCE: -4,
    PIPE_SPEED: 1.5,
    PIPE_GAP: 240,
    PIPE_SPAWN_RATE: 180, // frames between pipes - much more distance
    BIRD_X: 100,
    BIRD_SIZE: 34,
    CANVAS_WIDTH: 800,
    CANVAS_HEIGHT: 900,
    PIPE_WIDTH: 80,
};
